import { BrowserRouter } from 'react-router-dom'
import React from 'react'
import Router from './Router'

const App = () => (
    <BrowserRouter>
        <Router />
    </BrowserRouter>
)

export default App
